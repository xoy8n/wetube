extends ../base
include ../mixins/video



block content
    div.profile__data
        if user.avatarUrl
            img(src="/"+ user.avatarUrl).avatar
            h3=user.name
        else
            span.profile__avatar--emoji 😎
            h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 비디오를 찾을 수 없습니다.